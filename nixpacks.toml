[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["cd v1/backend && python -m venv /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = ". /opt/venv/bin/activate && cd v1/backend && uvicorn server:app --host 0.0.0.0 --port $PORT"
